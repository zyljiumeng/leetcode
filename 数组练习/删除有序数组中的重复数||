class Solution {
    public int removeDuplicates(int[] nums) {
    if (nums.length <= 2) return nums.length;
    
    int slow = 2; // 前两个元素一定保留
    for (int fast = 2; fast < nums.length; fast++) {
        // 比较当前元素与 slow-2 位置的元素
        if (nums[fast] != nums[slow - 2]) {
            nums[slow] = nums[fast];
            slow++;
        }
    }
    return slow;
}
}
